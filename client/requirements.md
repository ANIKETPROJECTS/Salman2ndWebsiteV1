## Packages
framer-motion | Complex animations for the hero section and timeline
lucide-react | Icons for the UI (already in base, but ensuring usage)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Racing Sans One'", "cursive"], // Or a similar Google Font for the racing theme
}
colors: {
  racing: {
    red: "#EF4444",
    black: "#111827",
    gray: "#F3F4F6",
  }
}
